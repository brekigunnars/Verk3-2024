import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
      padding: 10,
    },
    logo: {
      width: 100,
      height: 100,
      alignSelf: 'center',
    },
    headerText: {
      textAlign: 'center',
      marginVertical: 10,
      fontSize: 18,
    },
  });

export default styles;
